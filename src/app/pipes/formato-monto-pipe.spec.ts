import { FormatoMontoPipe } from './formato-monto-pipe';

describe('FormatoMontoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoMontoPipe();
    expect(pipe).toBeTruthy();
  });
});
